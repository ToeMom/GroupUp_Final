/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.britannica.com', 'www.nbc.com', 'img.olympics.com', 'i.ibb.co'],
      },
};

export default nextConfig;
